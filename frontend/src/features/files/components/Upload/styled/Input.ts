import { styled } from '@mui/material'

const Input = styled('input')({
	height: 1,
	overflow: 'hidden',
	position: 'absolute',
	bottom: 0,
	left: 0,
	whiteSpace: 'nowrap',
	width: 1,
	// display: 'none',
})

export default Input
